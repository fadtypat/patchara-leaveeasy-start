# ACL.md — ตารางสิทธิ์การใช้งาน LeaveEasy

อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2

| บทบาท (`role`) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 พนักงาน (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | อนุมัติหรือไม่อนุมัติใบลาของตัวเอง (แม้แต่ใบของตัวเองก็เปลี่ยนสถานะเองไม่ได้ ต้องรอหัวหน้า/ฝ่ายบุคคล) · ดูใบลาของพนักงานคนอื่น (เห็นได้เฉพาะใบของตัวเอง) · แก้ประเภทการลา |
| ✅ หัวหน้า (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ประเภทการลา |
| 🧾 ฝ่ายบุคคล (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบ ประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | — |

## สถานะการบังคับใช้จริงตอนนี้ (สัปดาห์ที่ 7)

ตารางนี้เป็น**เป้าหมาย**ตาม spec — โค้ดปัจจุบันยังไม่ได้บังคับตามนี้ทั้งหมด:

- Security Rules ตอนนี้มีแค่กฎขั้นต่ำ "ต้องล็อกอินก่อนถึงอ่าน/เขียนได้" (`request.auth != null`) — **ยังไม่แยกสิทธิ์ตามบทบาท** ผู้ใช้ที่ล็อกอินแล้วทุกคนยังกดอนุมัติ/ไม่อนุมัติ/ลบใบลาใดก็ได้ ไม่ว่าจะเป็นใบของตัวเองหรือไม่
- การแยกสิทธิ์ตามตารางนี้ (rules รายบทบาท + ทดสอบด้วยการสวมรอย) เป็นงานของ **สัปดาห์ที่ 8**
- หน้าจัดการประเภทการลา (`leave-types.html`) ยังไม่ได้ต่อกับ Firestore จริง (ยังแก้แค่ในหน่วยความจำ) จึงยังไม่มีการจำกัดว่าต้องเป็น `hr` เท่านั้นที่แก้ได้
- บัญชีที่สมัครผ่าน `signup.html` ตอนนี้ได้ `role: "employee"` เสมอ — ยังไม่มี UI สร้างบัญชี `manager`/`hr` ต้องตั้งค่าตรงใน Firestore Console เอง
